Desktop bundle icons used by Tauri packaging live here.

- `icon.png` is the shared desktop bundle icon.
- `icon.ico` is required for Windows MSI/resource generation.

Both files are derived from the repo MarkQL logo in `docs/assets/logo/`.
