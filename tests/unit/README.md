# Unit / Component Tests

Current C++ unit/component tests are still implemented in `tests/test_*.cpp` and executed via `markql_tests` + CTest.

This directory is reserved for staged migration of future C++ test files by category without breaking existing build/test wiring.
