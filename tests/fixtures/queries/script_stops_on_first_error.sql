SELECT section
FROM document
WHERE attributes.class CONTAINS 'result'
ORDER BY node_id ASC
LIMIT 1;

SELECT FROM document;

SELECT section
FROM document
WHERE attributes.class CONTAINS 'result'
ORDER BY node_id ASC
LIMIT 2;
